[package]
name = "sigil"
version = "2.1.2"
description = "sigil — system-wide trust verification for AGNOS"
license = "GPL-3.0-only"
repository = "https://github.com/MacCracken/sigil"
cyrius = "4.5.0"

[build]
src = "src/lib.cyr"
output = "build/sigil"
